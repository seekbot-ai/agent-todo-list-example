services:
  todo:
    build: .
    command: uvicorn todo_server.main:app --host 0.0.0.0 --port 8001
    ports: ["8001:8001"]
  agent:
    build: .
    command: uvicorn api_gateway.main:app --host 0.0.0.0 --port 8000
    ports: ["8000:8000"]
